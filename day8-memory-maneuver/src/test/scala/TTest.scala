
//class TTest extends WordSpec with Matchers {
//  "TDD" must {
//    "TDD" should {
//      "irecursive" in {
//        val input  = List(2, 3, 0, 3, 10, 11, 12, 1, 1, 0, 1, 99, 2, 1, 1, 2)
//        val actual = doit(input)
//        println(actual)
//      }
//    }
//  }

 //  def doit(data: List[Int]) = {
   // val node = List.empty[Node] :+ createNode(data)
   // node
  //}

 // def createNode()

//  def createNode(data: List[Int]): Node =
//    if (data.isEmpty) {
//      Node(List.empty[Node], List.empty[Int])
//    } else {
//      val quantitiyChildNodes = data.head
//      val quantityMeteaData   = data(1)
//
//      val restData = data.slice(2, data.size)
//
//      if (quantitiyChildNodes == 0) {
//        Node(
//          List.empty[Node],
//          restData.slice(0, quantityMeteaData)
//        )
//      } else {
//        createNode(restData.slice(2 + quantityMeteaData, restData.size))
//      }
//    }
//
//  def subList(original: List[Int], startPosition: Int): List[Int] = original.slice(startPosition, original.size)

}
