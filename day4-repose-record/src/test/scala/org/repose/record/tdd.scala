package org.repose.record
import org.scalatest.{ Matchers, WordSpec }

class tdd extends WordSpec with Matchers {
//  "GetInput" must {
//    val subject = new GetInput()
//    "parse a file line" should {
//      "extract a date" in {
//        val line   = FileReader.read("test-line-date.txt").get.head
//        val actual = subject.extractDate(line)
//        actual shouldBe LocalDateTime.of(1518, 3, 18, 0, 3)
//      }
//
//      "do Stuff" in {
//        val lines = FileReader.read("test-input.txt").get
//        val result = lines.map(item => subject.extractLine(item))
//        val sortedResult = subject.sortItems(result)
//
//        sortedResult.foreach(println)

//        for (line <- lines) {
//          val date    = subject.extractDate(line)
//          val content = subject.extractContent(line)
//          (date, content)
//        }
//      }
//    }
//  }

}
